
class OverrideCity {

public:

    bool operator==(const OverrideCity& o) const {
        return false;
    }

    OverrideCity& operator<<=(size_t i) {
        return *this;
    }

    char operator[](size_t i) const {
        return 'x';
    }

    void operator()() {
        abort();
    }

    operator bool() const {
        return false;
    }
};
