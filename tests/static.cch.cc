#include "tests/static.cch.h"

 int x = 0;

 int foo() {
    int m = 1;
    return m * 2;
}
     const int bar::cid = 347;
     const map<string, string> bar::m = { { "{", "}" } };
     const int bar::fnc = foo();
